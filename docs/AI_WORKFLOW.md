# AI workflow

- Cursor agent used for scaffolding, clean architecture structure, and test-first slices
- All generated code verified with `pnpm test` and `pnpm build`
- Security and complexity rules applied via ESLint and plan constraints
